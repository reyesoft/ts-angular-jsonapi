For development add this to your bower.json

```
"ts-angular-jsonapi": "git@github.com:reyesoft/ts-angular-jsonapi.git#develop",
```

```bash
# tag the commit
git tag -a v0.0.2 -m "Release version 0.0.2"

# push to GitHub
git push origin master --tags
```
